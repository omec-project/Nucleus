/*
 * Copyright 2019-present Infosys Limited
 *
 * SPDX-License-Identifier: Apache-2.0
 */
/******************************************************************************
 *
 * This is an auto generated file.
 * Please do not edit this file.
 * All edits to be made through template source file
 * <TOP-DIR/scripts/GtpV2StackCodeGen/tts/ietemplate.cpp.tt>
 ******************************************************************************/

#include "targetIdentificationIe.h"
#include "dataTypeCodecUtils.h"

TargetIdentificationIe::TargetIdentificationIe() 
{
    ieType = 121;
    // TODO

}

TargetIdentificationIe::~TargetIdentificationIe() {
    // TODO Auto-generated destructor stub
}

bool TargetIdentificationIe::encodeTargetIdentificationIe(MsgBuffer &buffer, TargetIdentificationIeData const &data)
{
    if (!(buffer.writeUint8(data.targetType)))
    {
        errorStream.add((char *)"Encoding of targetType failed\n");
        return false;
    }
    if (!(buffer.writeUint8(data.targetId)))
    {
        errorStream.add((char *)"Encoding of targetId failed\n");
        return false;
    }

    return true;
}

bool TargetIdentificationIe::decodeTargetIdentificationIe(MsgBuffer &buffer, TargetIdentificationIeData &data, Uint16 length)
{     
    // TODO optimize the length checks
    
    Uint16 ieBoundary = buffer.getCurrentIndex() + length;

    buffer.readUint8(data.targetType);
    if (buffer.getCurrentIndex() > ieBoundary)
    {
        errorStream.add((char *)"Attempt to read beyond IE boundary: targetType\n");
        return false;
    }

    buffer.readUint8(data.targetId);
    if (buffer.getCurrentIndex() > ieBoundary)
    {
        errorStream.add((char *)"Attempt to read beyond IE boundary: targetId\n");
        return false;
    }

    // The IE is decoded now. The buffer index should be pointing to the 
    // IE Boundary. If not, we have some more data left for the IE which we don't know
    // how to decode
    if (ieBoundary == buffer.getCurrentIndex())
    {
        return true;
    }
    else
    {
        errorStream.add((char *)"Unable to decode IE TargetIdentificationIe\n");
        return false;
    }
}
void TargetIdentificationIe::displayTargetIdentificationIe_v(TargetIdentificationIeData const &data, Debug &stream)
{
    stream.incrIndent();
    stream.add((char *)"TargetIdentificationIeData:");
    stream.incrIndent();
    stream.endOfLine();
  
    stream.add((char *)"targetType: ");
    stream.add(data.targetType);
    stream.endOfLine();
  
    stream.add((char *)"targetId: ");
    stream.add(data.targetId);
    stream.endOfLine();
    stream.decrIndent();
    stream.decrIndent();
}
